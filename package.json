{
  "name": "video-frame-overlay-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "npm run build:shared && cd backend && npm run build",
    "build:shared": "cd shared && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../shared && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
